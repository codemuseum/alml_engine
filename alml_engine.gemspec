Gem::Specification.new do |s|
  s.name = %q{alml_engine}
  s.version = "0.0.2"

  s.required_rubygems_version = Gem::Requirement.new(">= 0") if s.respond_to? :required_rubygems_version=
  s.authors = ["ThriveSmart, LLC"]
  s.date = %q{2008-10-19}
  s.description = %q{The engine that compiles Alml (abstract layout markup language) to html.}
  s.email = %q{developers@thrivesmart.com}
  s.extra_rdoc_files = ["README.textile", "LICENSE"]
  s.files = ["LICENSE", "README.textile", "Rakefile", "lib/alml_engine.rb"]
  s.has_rdoc = true
  s.homepage = %q{http://www.github.com/thrivesmart/alml_engine}
  s.require_paths = ["lib"]
  s.rubyforge_project = %q{alml_engine}
  s.rubygems_version = %q{1.2.0}
  s.summary = %q{The engine that compiles Alml (abstract layout markup language) to html.}

  if s.respond_to? :specification_version then
    current_version = Gem::Specification::CURRENT_SPECIFICATION_VERSION
    s.specification_version = 2

    if current_version >= 3 then
    else
    end
  else
  end
end
